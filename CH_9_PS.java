/*class cylinder {

    private int radius;
    private int height;

    void setRadius(int radius) {
        this.radius = radius;
    }

    int getRadius() {
        return radius;
    }

    void setHeight(int height) {
        this.height = height;
    }

    int getHeight() {
        return height;
    }

    double getSurfaceArea() {
        return (2 * Math.PI * radius * height) + (2 * Math.PI * radius * radius);
    }

    double getVolume() {
        return Math.PI * radius * radius * height;
    }

    cylinder(int radius, int height) {

        this.radius = radius;
        this.height = height;
    }
}

class rectangle {

    private int Length;
    private int Breadth;

    rectangle() {

        this.Length = 4;
        this.Breadth = 5;
    }

    rectangle(int Length, int Breadth) {
        this.Length = Length;
        this.Breadth = Breadth;
    }

    int getLength() {
        return Length;
    }

    int getBreadth() {
        return Breadth;
    }    
    }

    class sphere{
        private int radius;

        sphere(int radius){
            this.radius=radius;
        }

        int getRadius(){
            return radius;
        }

        double surfaceArea(){
            return 4*Math.PI*radius*radius;
        }

        double volume(){
            return (1.33333333333) * (Math.PI) * (radius * radius * radius);
        }
    }

public class CH_9_PS {
    public static void main(String[] args) {

        /* Use Math.PI to get accurate value of Pie. */

        // Problem 1 --> To set and get radius and height of a cylinder.
    
        /* cylinder obj1 = new cylinder(9,12);
         * 
         * // obj1.setRadius(9);
         * // obj1.setHeight(12);
         * 
         * System.out.println(obj1.getRadius());
         * System.out.println(obj1.getHeight());
         * 
         * //Problem 2 --> Calculate surface area and volume of cylinder.
         * 
         * System.out.println(obj1.getSurfaceArea());
         * System.out.println(obj1.getVolume());*/
         
        // Problem 3 --> To set and get length and breadth of rectangle.

       /*  rectangle obj2 = new rectangle(10,25);

           System.out.println(obj2.getLength());
           System.out.println(obj2.getBreadth());

        //    Problem 4 --> Set and get radius of sphere and calculate surface area and volume.

        sphere obj3 = new sphere(2);

        System.out.println(obj3.getRadius());
        System.out.println(obj3.surfaceArea());
        System.out.println(obj3.volume());

    }
}
 */